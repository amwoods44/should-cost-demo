# Cost Benchmark — Demo

Google REWS comparable-based cost benchmark. Single-file static app.

- **App:** `index.html` (`/`)
- **Overview deck:** `deck.html` (`/deck.html`)
- **Corpus:** `projects.csv` — representative portfolio data, not live REWS actuals.

Hosted via GitHub Pages.
